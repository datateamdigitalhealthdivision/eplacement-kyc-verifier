# Langflow Flow

Import `evidence_verification_flow.json` into Langflow and keep the repository root on `PYTHONPATH` so Langflow can discover the custom components under `src/langflow_components/`.

For the full explanation of the component chain and where each node maps into the Python codebase, see [docs/langflow.md](../docs/langflow.md).
